import { DateMessagesPipe } from './date-messages.pipe';

describe('DateMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new DateMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
